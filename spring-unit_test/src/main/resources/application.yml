spring:
  application:
    name: spring-unit_test

  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: 180990

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
