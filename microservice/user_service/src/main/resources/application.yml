spring:
  application:
    name: user_service
  datasource:
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: 180990
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

server:
  port: 8081